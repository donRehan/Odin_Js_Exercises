SCRATCH PAD
-----------

leap year => {
	Divisible by 4 {
		Divisible by 100 {
			Divisible by 400 {
			return true
			}
			return false
		}
		return true;
	}
	return flase
}

Divisible by 4 ? (Divisible by 100 ? (Divisible by 400 ? return true : return false; ) : return true) : return false;

year => {
	Divisible by 4 ?
	(Divisible by 100 ? 
	(Divisible by 400 ? 
	return true 
	: return false; ) 
	: return true) 
	: return false;
}
